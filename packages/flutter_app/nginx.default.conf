server {
  listen 80;
  server_name _;
  root /usr/share/nginx/html;

  location / {
    try_files $uri $uri/ /index.html;
  }

  # Optional: health check endpoint (served if index exists)
  location = /healthz {
    add_header Content-Type text/plain;
    return 200 'ok';
  }
}
