# Backend service (Django)

This package contains the Django backend for yetAnotherBoilerplate.

- Dependencies are managed with Poetry via `pyproject.toml`.
- Local development defaults to SQLite; use `DATABASE_URL` for Postgres.
- Custom user model uses email as the username.
